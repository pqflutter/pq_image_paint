import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pq_image_paint/pq_image_paint.dart';

void main() {
  test('adds one to input values', () {
    final calculator = Container(child: CustomPaint(painter: PQImagePaint()),);
  });
}
